.vscode/

__pycache__
.venv
.env
/raw*
/logs/

.pytest_cache

/workspaces/
/.cerebras_budget.json
/.cerebras_budget.json.lock

*.md
!README.md

# Not this branch's, but this working tree is shared with `advanced-web`: `git switch` leaves
# its build output, its node_modules and its UX-audit captures behind, and untracked noise on
# every status is how a real leftover stops being noticed.
/web/
/auditoria-ux/
/.maintenance.json
