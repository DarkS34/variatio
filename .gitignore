.vscode/

__pycache__
.venv
.env
/raw*
/logs/

.pytest_cache

/workspaces/
/.cerebras_budget.json
/.cerebras_budget.json.lock

*.md
!README.md

# Not this branch's, but a working tree is often shared with `variatio-web`: switching back
# leaves its build output, its node_modules and its working captures behind, and untracked
# noise on every status is how a real leftover stops being noticed.
/web/
/auditoria-ux/
/.maintenance.json
