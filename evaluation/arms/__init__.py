"""The three architectures under comparison, one module each.

Kept empty of imports: `evaluation.run_arm` reaches them lazily, and each arm imports the
evaluation's contract back from the package above.
"""
